import nodemailer from 'nodemailer'
function getT(){ const u=process.env.GMAIL_USER,p=process.env.GMAIL_APP_PASSWORD; if(!u||!p){console.warn('[EMAIL] creds not set');return null}; return nodemailer.createTransport({service:'gmail',auth:{user:u,pass:p}}) }
export async function sendEmail(o:{to:string;subject:string;html:string}):Promise<boolean>{ const t=getT(); if(!t)return false; try{await t.sendMail({from:`"OmniCraft Studios" <${process.env.GMAIL_USER}>`,to:o.to,subject:o.subject,html:o.html});return true}catch(e){console.error('[EMAIL]',e);return false} }
export function newLeadEmail(o:{name:string;email:string;phone:string;company?:string;summary:string;slug:string;appUrl:string}){ return{subject:`New Lead: ${o.name}${o.company?` — ${o.company}`:''}`,html:`<div style="font-family:system-ui,sans-serif;max-width:560px;margin:0 auto;padding:24px;color:#1a1a18"><h2 style="font-weight:300;margin:0 0 16px">New OmniCraft Lead</h2><table style="font-size:14px;border-collapse:collapse"><tr><td style="color:#6b6860;padding:3px 12px 3px 0">Name</td><td><strong>${o.name}</strong></td></tr>${o.company?`<tr><td style="color:#6b6860;padding:3px 12px 3px 0">Company</td><td>${o.company}</td></tr>`:''}<tr><td style="color:#6b6860;padding:3px 12px 3px 0">Email</td><td><a href="mailto:${o.email}">${o.email}</a></td></tr><tr><td style="color:#6b6860;padding:3px 12px 3px 0">Phone</td><td>${o.phone}</td></tr></table>${o.summary?`<p style="margin-top:16px;font-size:14px;line-height:1.7;background:#f4f2ec;padding:12px;border-radius:8px">${o.summary}</p>`:''}<a href="${o.appUrl}/admin" style="display:inline-block;margin-top:16px;background:#1a1a18;color:#f4f2ec;text-decoration:none;padding:10px 20px;border-radius:8px;font-size:13px">Review in Admin →</a></div>`} }
export function clientConfirmEmail(o:{name:string;slug:string;appUrl:string}){ const f=o.name.split(' ')[0]; return{subject:`Your OmniCraft roadmap is being reviewed, ${f}`,html:`<div style="font-family:system-ui,sans-serif;max-width:560px;margin:0 auto;padding:24px;color:#1a1a18"><h2 style="font-weight:300">Your roadmap is in review, ${f}.</h2><p style="color:#6b6860;font-size:14px;line-height:1.7">The Studio Owner will return with a finalised scope and quote within 24–48 hours.</p><div style="background:#f4f2ec;border-radius:8px;padding:14px;margin:16px 0"><p style="font-size:11px;font-family:monospace;color:#6b6860;margin-bottom:6px">YOUR DASHBOARD</p><a href="${o.appUrl}/p/${o.slug}" style="font-size:13px;font-family:monospace;color:#1a1a18">${o.appUrl}/p/${o.slug}</a></div><a href="${o.appUrl}/p/${o.slug}" style="display:inline-block;background:#1a1a18;color:#f4f2ec;text-decoration:none;padding:10px 20px;border-radius:8px;font-size:13px">View Dashboard →</a></div>`} }
export function roadmapReadyEmail(o:{name:string;slug:string;appUrl:string;amount?:number;currency?:string}){ const f=o.name.split(' ')[0]; return{subject:`Your OmniCraft roadmap is ready, ${f}`,html:`<div style="font-family:system-ui,sans-serif;max-width:560px;margin:0 auto;padding:24px;color:#1a1a18"><h2 style="font-weight:300">Your roadmap is ready, ${f}.</h2><p style="color:#6b6860;font-size:14px;line-height:1.7">The Studio has prepared your full project roadmap${o.amount?` with a quote of <strong>${o.currency||'NGN'} ${o.amount.toLocaleString()}</strong>`:''}.</p><a href="${o.appUrl}/p/${o.slug}" style="display:inline-block;margin-top:16px;background:#1a1a18;color:#f4f2ec;text-decoration:none;padding:10px 20px;border-radius:8px;font-size:13px">View Your Roadmap →</a></div>`} }
export function humanReviewEmail(o:{name:string;email:string;slug:string;message:string;appUrl:string}){ return{subject:`Review Requested: ${o.name}`,html:`<div style="font-family:system-ui,sans-serif;max-width:560px;margin:0 auto;padding:24px;color:#1a1a18"><h2 style="font-weight:300">Client requests a human</h2><p style="color:#6b6860;font-size:14px"><strong>${o.name}</strong> (${o.email})</p>${o.message?`<p style="background:#f4f2ec;border-radius:8px;padding:12px;font-size:14px;line-height:1.7">${o.message}</p>`:''}<a href="${o.appUrl}/admin" style="display:inline-block;margin-top:16px;background:#1a1a18;color:#f4f2ec;text-decoration:none;padding:10px 20px;border-radius:8px;font-size:13px">Jump into Admin →</a></div>`} }
